# ===============================
# 📦 BASIC RUNTIME CONFIGURATION
# ===============================

COMPOSE_PROJECT_NAME=monitoring_stack

# Domain name or hostname where monitoring stack is accessible
HOSTNAME_MONITOR=monitor.example.com

# External port to expose (443 recommended)
LISTEN_PORT=443


# ===============================
# 🔐 TLS CONFIGURATION
# ===============================
# USE_SELF_SIGNED_TLS=true → setup.sh auto-generates self-signed certs
# USE_SELF_SIGNED_TLS=false → you must provide tls.crt + tls.key in ./secrets
USE_SELF_SIGNED_TLS=true
USE_TLS=${USE_SELF_SIGNED_TLS}


# ===============================
# 📧 ALERTMANAGER (EMAIL ALERTS)
# ===============================

# SMTP server & credentials
ALERT_SMTP_SMARTHOST=smtp.gmail.com:587
ALERT_SMTP_FROM=monitoring@gmail.com
ALERT_SMTP_USER=monitoring@gmail.com

# Using Gmail? Use an App Password here (never your real password)
ALERT_SMTP_PASS=your_app_password_here

# Where Alertmanager sends notifications
ALERT_EMAIL_TO=alerts@example.com

# Alert grouping and repeat intervals
ALERT_GROUP_WAIT=30s
ALERT_GROUP_INTERVAL=5m
ALERT_REPEAT_INTERVAL=3h


# ===============================
# 📊 GRAFANA DEFAULT ADMIN USER
# ===============================
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=changeme


# ===============================
# 🗂️ PATHS & INTERNAL SETTINGS
# ===============================

# TLS certificate paths (used by setup.sh)
TLS_CRT_PATH=./secrets/tls.crt
TLS_KEY_PATH=./secrets/tls.key

# Since we're using env-based SMTP, this secret file isn't needed
# (kept here for future compatibility)
SMTP_PASS_SECRET_PATH=./secrets/smtp_pass

# Internal flag (kept for compatibility with setup.sh and docker-compose)
USE_SECRET_SMTP=false

# Config file used by Alertmanager container
ALERTMANAGER_CONFIG_FILE=alertmanager.yml
